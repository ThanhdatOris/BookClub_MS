<form method="post" action="{{ path('app_attendances_delete', {'id': attendance.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ attendance.id) }}">
    <button class="btn">Delete</button>
</form>
