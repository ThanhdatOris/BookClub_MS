{% extends 'base.html.twig' %}

{% block title %}Activities{% endblock %}

{% block body %}
    <h1>Activities</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ activity.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ activity.title }}</td>
            </tr>
            <tr>
                <th>Descriptiton</th>
                <td>{{ activity.descriptiton }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ activity.date ? activity.date|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Time</th>
                <td>{{ activity.time ? activity.time|date('H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Location</th>
                <td>{{ activity.location }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ activity.status }}</td>
            </tr>
            <tr>
                <th>Created_at</th>
                <td>{{ activity.createdAt }}</td>
            </tr>
            <tr>
                <th>Updated_at</th>
                <td>{{ activity.updatedAt ? activity.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_activities_index') }}">back to list</a>

    <a href="{{ path('app_activities_edit', {'id': activity.id}) }}">edit</a>

    {{ include('activities/_delete_form.html.twig') }}
{% endblock %}
