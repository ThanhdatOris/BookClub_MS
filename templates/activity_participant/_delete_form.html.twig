<form method="post" action="{{ path('app_activity_participant_delete', {'id': activity_participant.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ activity_participant.id) }}">
    <button class="btn">Delete</button>
</form>
